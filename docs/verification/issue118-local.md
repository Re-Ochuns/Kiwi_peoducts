# Issue #118 ローカル検証

## 対象

- ブランチ：`codex/118-board-order-plan`
- ベース：develop `316959a3c7897f9a3080d5e29c5f93ae47cfd61e`
- 作業ツリー：`/home/kugis/products/hackathon/kiwi-verification/issue118`
- 検証対象の実装：この文書を含むコミット。最終コミットIDは `git rev-parse HEAD` で確認する。

## 結果

- `make check`（Flutter/Dart 3.47.2に付属する実行環境を指定）：静的解析、整形確認、337テスト成功、既存1件スキップ。
- 追加画面テスト：競合後の顧客・配送先の保持、既存ロットの担当者・場所の保持を含む9件。
- 既存Golden：16件成功。Webリリースビルド成功。
- 専用DBの全28ファイル／1,127アサーション成功。候補判定の補強後、単独テスト31件（マスター不足、要確認元在庫、期限の境界を含む）とSQL lintを再実行。
- 2つのDBセッションを実際に競合させ、一方のコミットを待った要求が再検証されること、予約6kgのまま増えないこと、敗者の受注・計画が残らないことを確認。
- ローカル認証付きHTTP API：有効マスター取得、新規受注・計画の同時確定、冪等再送、物理重量不変、割当量、既存予備ロットの受注移管と残量を確認。
- Widgetテストでサイドバー幅224pxを含む900px・1280pxを再現し、入力から登録完了まで確認。
- ブラウザー自動操作はホスト側の接続エラーで実行できなかった。ブラウザーでの手動操作を自動確認済みとはしない。

## 手動プレビュー

`http://localhost:58084/`（127.0.0.1に限定）。既存の58083とは別サービス。

1. 工程ボードを開き「在庫」を選ぶ。
2. 出荷予定日 `2027-06-01`、品種「ヘイワード」、等級「L」、注文量 `1` を指定して候補を表示。
3. 冷蔵コンテナ、または予備ロットをクリック。
4. 顧客・配送先「Dummy」を選ぶ。冷蔵コンテナの場合は担当作業者も選ぶ。
5. 確認して「受注・計画を確定」。受注番号と計画番号を確認してボードへ戻る。
6. 受注一覧・追熟計画で登録内容を確認。新規計画は使用量と同量、既存ロットは同じ計画番号で予備量だけ減る。

API検証直後の残量は冷蔵4.5kg、既存ロット予備2kg。手動登録により変わる。出荷基準は日本時間9時。実作業開始は行っていない。

## 環境分離と再現時の注意

専用Supabaseプロジェクト `kiwi_issue118`、API59321、DB59322を使用。既存 `kiwi_products` のDBは初期化・変更していない。DBを初期化する前はプロジェクトID、ポート、作業ツリー変更を必ず確認する。

既存DBテストのdblink接続先に `supabase_db_kiwi_products` の固定名があるため、専用DBコンテナ内だけでこの名前を自身のDocker IPへ解決させた。実際の既存DBには接続しない。

検証用のメール認証、認証フィクスチャ、接続定義、起動エントリーポイントは `local-runtime/` 内だけの設定。製品のGoogle認証コードと追跡対象のSupabase設定は変更していない。認証情報や検証ビルドはコミットしない。

DBテスト後に手動プレビュー用フィクスチャを作成した。全DBテストの再実行にはクリーンな専用DBが必要であり、プレビュー操作中に無断でリセットしない。
