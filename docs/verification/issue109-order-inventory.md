# Issue #109 受注時予約の検証

- 実装ブランチ: `codex/order-inventory-reservation` / PR #110（仕様のみのPRを実装込みへ更新）
- 実装対象: 在庫確認・選択 → 受注入力 → 原子的な受注確定と在庫予約 → 追熟計画への引き継ぎ。
- 検証環境: WSL Ubuntu、Flutter 3.47.2 / Dart 3.13.2 / Node 24.19.0 / Supabase CLI 2.117.0。
- アプリ検証: `kiwi-verification/issue109-final`、コード `341e04e` と非有限数ガードの補正。元の検証ツリーと既存変更は保持。
- DB検証: 独立プロジェクト `kiwi_issue109`、コンテナ `supabase_db_kiwi_issue109`、ポート59322。ほかのDBは初期化していない。
- 既存並行テストの固定ホスト名は検証コピー内だけ `supabase_db_kiwi_issue109` へ置換。

## 結果

- `git diff --check` 成功。
- `make check` 成功: バージョン確認・format・analyze・Flutter 294件成功、既存1件skip。
- Golden 18件成功。900/1280pxの在庫選択画像を追加、受注一覧の「未計画」表示を更新。画像で折り返し・切れ・操作位置を確認。
- DB 27ファイル・1119件成功。追加42件を含む。
- Webリリースビルド成功。最終CIの結果はPRの検証欄へ記載。
- 非有限数・極端な数値・小数桁超過を含む最終画面テスト2件も成功。

## 数量と状態の確認

10kgのコンテナで4kg受注 → 在庫予約4kg。2kgを計画へ引き継ぎ、予備1kg追加 → 予約総量5kg。計画取消 → 受注予約4kgを保持。受注増量5kg → 予約5kg。不足する11kgへの変更 → 受注・予約とも変更前に戻る。

別受注2kgと予備を同じコンテナの計画へ追加し、一受注を取り消しても他受注と予備の予約を保持することを確認。注入では所有者が分かれた予約を合計し、元在庫イベント1件で減算し、ラベルを生成する。

実DBの2接続で確定を競合させ、ロック待機後の再検証と、同一冪等性キーの同時送信が同じ受注を返すことを確認。

## 配備・未確認事項

- DB migration → 新アプリの順に配備し、強制リロード後に在庫選択から確認する。現在は未配備。
- 移行前受注・予約の所有者を推測して変更しない。新規の旧クライアント経路は下書き保存までで、予約なしの確定を拒否する。
- ブラウザからの実操作、Stagingでの現場受入、担当領域のレビューは未実施。自動テストで代替完了とはしていない。
- 注入テストは既存契約どおり、収穫年度・月と追熟マスターを明示した計画を使用。既存画面の収穫年度・月入力は今回の変更範囲外。
